
import React from "react";

export const metadata = {
  title: "Payment | TeamCraft",
  description: "Make a payment for your order.",
};

const page = ({ params }) => {
  const id = params.id;
  return (
    <>

    </>
  );
};

export default page;
