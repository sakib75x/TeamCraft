import Calendar from "@/components/Calendar/Calendar";

export const metadata = {
    title: "Calendar | TeamCraft",
    description: "Calendar for TeamCraft",
}

const CalendarPage = () => {
    return (
        <div>
            <Calendar></Calendar>
        </div>
    );
};

export default CalendarPage;